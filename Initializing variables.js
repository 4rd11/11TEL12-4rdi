"use strict";
let nilaiIPA = 85;                      // Mendeklarasikan nilai IPA
let nilaiPraktikumIPA = nilaiIPA;      // Menyalin nilai IPA untuk nilai praktikum
let nilaiIPS;                         // Mendeklarasikan variabel nilai IPS tanpa nilai awal

console.log(nilaiIPA);              // -> 85 (Menampilkan nilai IPA)
console.log(nilaiPraktikumIPA);    // -> 85 (Menampilkan nilai praktikum IPA)
nilaiIPS = 90;                    // Memberikan nilai IPS
console.log(nilaiIPS);           // -> 90 (Menampilkan nilai IPS)
